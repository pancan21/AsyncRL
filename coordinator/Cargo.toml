[package]
name = "coordinator"
version = "0.1.0"
edition = "2021"

[dependencies]
futures = "0.3"
common = { path = "../common" }
num = "0.4"
bytemuck = "1"

[dev-dependencies]
lattice_cho = { path = "../lattice_cho" }
pyo3 = { version = "0.22", features = ["abi3-py312"] }
